<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Reset your Edutrack Password</title>
    <style>
      body { margin:0; padding:0; background:#f4f7fb;
        font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,Helvetica,Arial,sans-serif; }
      .card { background:#fff; border-radius:16px; box-shadow:0 4px 20px rgba(0,72,170,0.08);
              max-width:600px; margin:40px auto; padding:40px; }
      .logo { text-align:center; font-size:22px; font-weight:700; color:#0056d2; }
      .button { display:inline-block; background:linear-gradient(90deg,#0056d2,#3b82f6);
                color:#fff!important; text-decoration:none; padding:14px 28px;
                border-radius:10px; font-weight:600; box-shadow:0 6px 18px rgba(59,130,246,0.3);
                margin:20px auto; }
      .link { color:#3b82f6; word-break:break-all; text-decoration:underline; }
      .footer { text-align:center; margin-top:30px; font-size:13px; color:#94a3b8; }
    </style>
  </head>
  <body>
    <div class="card">
      <div class="logo">🎓 Edutrack</div>
      <h2 style="text-align:center;">Reset your password</h2>
      <p style="text-align:center;">
        Click the button below to set a new password for your account.
      </p>
      <p style="text-align:center;">
        <a href="<%= resetUrl %>" class="button">Reset my password</a>
      </p>
      <p style="font-size:13px;color:#64748b;text-align:center;">
        If the button doesn’t work, paste this link in your browser:<br />
        <a href="<%= resetUrl %>" class="link"><%= resetUrl %></a>
      </p>
      <div class="footer">
        This link expires in <%= expiresIn %>.<br/>
        If you didn’t request a reset, you can ignore this email.<br/>
        © <%= year %> Edutrack
      </div>
    </div>
  </body>
</html>
